<h1>Geo Selector</h1>

<%= content_for :demo_title, "Refines GEO options based on user selections" %>
<%= render_demo "/geo_selectors/demo" %>

<%= render_explanation do %>
  <p>
    <strong>No JavaScript was written for this demo.</strong>
    It uses the <code>data-reflex</code> attribute to produce the behavior.
  </p>
<% end %>

<%= render_gists(
  "app/views/geo_selectors/_demo.html.erb",
  "app/reflexes/geo_selector_reflex.rb"
) %>
