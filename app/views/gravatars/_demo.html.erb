<div class="grid grid-cols-2 gap-6">
  <div class="form-control">
    <input id="email" type="text" placeholder="Enter an email address..." class="form-input" data-reflex="input->GravatarReflex#perform" />
  </div>
  <span class="ml-3"><%= image_tag @gravatar_image_url || "" %></span>
</div>
